"""
LLM 結構化輸出的 Schema (Pydantic Models)。
這些模型專門用於傳遞給 LLM (如 OpenAI/Gemini) 作為 response_format 限制輸出格式。

Schemas (Pydantic models) for LLM structured output. These models are passed
to the LLM (e.g. OpenAI/Gemini) as response_format to constrain its output.
"""
