"""Telegram Bot 狀態機 (FSM) 處理器套件。

此套件包含所有需要透過 FSM (Finite State Machine) 進行多步驟互動的 Telegram 指令流程。
每個流程都封裝在獨立的模組中，並以 `_fsm.py` 結尾。

Telegram Bot state machine (FSM) handlers package.

This package contains every Telegram command flow that requires multi-step
interaction through a finite state machine. Each flow is encapsulated in
its own module, whose filename ends with `_fsm.py`.
"""
