請為以下日文自動詞與他動詞對，解析並生成高品質的語言學習材料。

【目標動詞對】
自動詞：{{ intransitive }}
他動詞：{{ transitive }}

【原始情境上下文】
{{ context_text }}

請嚴格遵守 JSON Schema 輸出結構化資料。包含以下兩個部分的內容：

1. Context (上下文情境)
請根據提供的原始情境上下文，重構成一段自然的雙人對話（LINE 風格）。
- 提供一段簡短的情境概要（中文）。
- 對話中必須自然包含目標動詞（自動詞或他動詞皆可，請根據情境決定最適合的一個）。
- 包含目標動詞的句子，請將 `is_target` 設為 true。
- `align` 欄位請交替使用 'left' 和 'right'。
- `audio` 請預設為空字串 `""`，`avatar` 請預設為 `"none"`。

2. Cloze (克漏字例句)
請從上述對話中，挑選那句包含目標動詞的最關鍵句子，製作成克漏字考題。
- 挖空時【絕對不能挖掉名詞】，名詞必須保留作為作答提示。
- 僅挖空與該動詞強相關的「助詞」及「動詞本身」。助詞請替換為 `_`，動詞及其前綴助詞替換為 `____`。
- 範例 1 (多重助詞)：原句「麺にソースを絡める」，挖空後必須是「麺_ソース____」（保留名詞「麺」與「ソース」，挖掉「に」與「を絡める」）。
- 範例 2 (單一助詞)：原句「ドアを開ける」，挖空後必須是「ドア____」（保留名詞「ドア」，挖掉「を開ける」）。
- `target_particle_verb` 請填寫實際被挖空部分的正確答案（例如：「に、を絡める」或「を開ける」），這將顯示在卡片背面的解答區。
- `verb_type_used` 必須明確標示該句使用的是 `'intransitive'`（自動詞）還是 `'transitive'`（他動詞）。
- `speaker` 與 `avatar` 必須與剛才生成的對話中，講出這句話的角色一致。
- `cloze_sentence` 填入上述按照規則挖空後的句子。
- 提供該句精準的中文翻譯。
