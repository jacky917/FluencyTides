"""
Template Validators Package.

此套件專門存放用來驗證 Anki 樣板的「單一插入測試入口」。
每個驗證器應專注於生成特定模型的一張包含所有邊界情況假資料的卡片，
以便在 Anki 中預覽 UI 和 Deep Link 的正確性。

This package hosts single-insert test entry points for validating Anki
templates. Each validator focuses on generating one card of a specific
model, populated with edge-case fake data, so the UI and deep links can
be previewed in Anki.
"""
