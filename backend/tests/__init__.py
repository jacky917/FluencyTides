"""
後端自動化測試套件。

Backend automated test suite.

注意：原第三輪測試基線（48 測試）位於未合併分支
claude/distracted-borg-2cfed8；本目錄由 STT 雙模式實作重新起步，
待該分支合併後整併。

Note: the original round-3 test baseline (48 tests) lives on the
unmerged branch claude/distracted-borg-2cfed8; this directory restarts
with the STT dual-mode implementation and will be consolidated once
that branch is merged.
"""
